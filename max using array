#include <stdio.h>

int main()
{
    int arr[5]={3,9,5,8,2};
    int max;
    max=arr[0];
    for(int i=1; i<5; i++)
    {
        if(max<arr[i])
           max=arr[i];
    }
    printf("%d",max);
    

    return 0;
}
