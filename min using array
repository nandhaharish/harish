#include <stdio.h>

int main()
{
    int arr[5]={3,9,5,8,2};
    int min;
    min=arr[0];
    for(int i=1; i<5; i++)
    {
        if(min>arr[i])
           min=arr[i];
    }
    printf("%d",min);
    

    return 0;
}
